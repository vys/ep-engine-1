#! /bin/sh

root="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd ..&& pwd )"/lib/python
if test -z "${PYTHONPATH}"; then
    PYTHONPATH=$root
else
    PYTHONPATH=$root:${PYTHONPATH}
fi
export PYTHONPATH
exec python26 $root/`basename $0` "$@"
